<UserControl x:Class="EggFarmSystem.Client.Modules.MasterData.Views.View"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="500">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <StackPanel Margin="0,30,0,0">
            <ToggleButton Style="{StaticResource SidebarButton}" 
                          IsChecked="{Binding Path=IsHenInput, Mode=TwoWay}"
                          x:Name="tglAyam" Click="tglAyam_Click">AYAM</ToggleButton>
            <ToggleButton Style="{StaticResource SidebarButton}"
                          IsChecked="{Binding Path=IsHouseInput, Mode=TwoWay}"
                          x:Name="tglKandang" Click="tglKandang_Click">KANDANG</ToggleButton>
            <ToggleButton Style="{StaticResource SidebarButton}" 
                          IsChecked="{Binding Path=IsEmployeeInput, Mode=TwoWay}"
                          x:Name="tglKaryawan" Click="tglKaryawan_Click">KARYAWAN</ToggleButton>
        </StackPanel>
        <ScrollViewer x:Name="scrMasterContent" Grid.Column="1">
            
        </ScrollViewer>
    </Grid>
</UserControl>
